const size = {
  lg: "1024px",
  md: "600px",
  sm: "375px"
}

const device = {
  lg: `(max-width: ${size.lg})`,
  md: `(max-width: ${size.md})`,
  sm: `(max-width: ${size.sm})`,
}

export { size, device };